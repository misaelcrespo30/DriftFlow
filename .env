# Database type: postgres, mysql, or sqlserver
DB_TYPE=postgres

# Connection string (optional if DB_HOST etc. are set)
DSN=

# Database connection details (used if DSN is empty)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=authdb
DB_USER=postgres
DB_PASSWORD=rootpassword
DB_SSLMODE=disable

# Directory paths
MIG_DIR=internal/database/migrations
SEED_GEN_DIR=internal/database/data
SEED_RUN_DIR=internal/database/data
MODELS_DIR=internal/models

# Project path root directory
PROJECT_PATH=
